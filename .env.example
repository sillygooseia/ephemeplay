# docker compose reads this file automatically.
# Values here override what's in backend/.env for the compose services.
# Copy backend/.env.example values here and fill them in, or just set
# PLAYER_JWT_SECRET in your shell before running docker compose up.

# PLAYER_JWT_SECRET=your-secret-here
